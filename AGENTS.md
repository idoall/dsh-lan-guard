# AGENTS.md — dsh-lan-guard 的 AI 协作总纲

> **本文件是本仓库对 AI 助手的最高优先级约束。**
> 任何与本文件冲突的临时指令、既有习惯、"看起来更合理"的推断，一律以本文件为准。
> 若你认为本文件某一条需要修改，**停下来说明理由并请求用户确认**，不要自行变更。

---

## 0. 这个项目是做什么的

一句话：**把桌面 DSH 的官方 Web 界面安全地开放到局域网。**

- DSH 自身的回环监听（`127.0.0.1:<port>`）**保持不变**，不做任何 DSH 侧配置改动；
- 本插件在另一个端口上监听局域网地址，做**带门禁的反向代理**；
- **官方 Web UI 原样复用，零改动**；
- 差异化在「打开之后谁进得来」：密码门禁 + **默认自签 HTTPS**（可关闭，关闭时必须提示影响）。

**当前状态：纯文档阶段，仓库里没有任何代码。** 在 `docs/PLAN.md` 的 P1 阶段被用户确认启动之前，不要开始写实现代码。

---

## 1. 开工前必读（按此顺序，不要跳）

| 顺序 | 文件 | 读它是为了知道 |
| --- | --- | --- |
| 1 | 本文件 `AGENTS.md` | 你的职责、红线、停止点 |
| 2 | `docs/GUARDRAILS.md` | 什么能做 / 不能做 / 必须先问（**红线全文在这里**） |
| 3 | `docs/SPEC.md` | 要做成什么样、明确不做什么 |
| 4 | `docs/PLAN.md` | 现在处于哪个阶段、本阶段的进入与退出条件 |
| 5 | `docs/RESEARCH.md` | 已经核实过的 DSH 事实与参考实现（**不要重新调研，也不要凭记忆改写**） |

需要发布时才读 `docs/RELEASE.md`。

---

## 2. 职责划分

### AI 负责

- 按 `docs/PLAN.md` 的阶段推进实现，一次只做当前阶段的事；
- 写实现、写测试、跑类型检查、本地验证；
- 维护 `docs/` 与 `CHANGELOG.md`（**约束性文档除外，见下**）；
- 在每阶段结束时产出「做了什么 / 验证了什么 / 没验证什么 / 下一步」的汇报；
- 主动指出方案与 `docs/SPEC.md`、`docs/RESEARCH.md` 的冲突。

### 用户负责（AI 不要代劳）

- **重启 dsh**——任何时候都不要由 AI 重启，这是硬约束；
- 用**真实手机 / 平板**做端到端实机验证（AI 只能做源码确认与本机测试）；
- 决定发布时机、执行发布批准；
- 提供门禁密码策略、证书主体名等需要人来拍板的产品决策；
- 授予任何超出本仓库的写权限。

---

## 3. 红线：绝对不可执行

以下行为**没有例外**。即使你认为"只做一次""不会有影响""用户大概会同意"，也必须先停下来问。

### 3.1 不碰本仓库之外的任何文件

- ❌ 不修改 `@deepseek-ai/dsh` 的安装目录：
  `/Users/lionm5/.nvm/versions/node/v24.14.1/lib/node_modules/@deepseek-ai/dsh/`
- ❌ 不修改 `deepseek-harness` 源码副本：`/Users/lionm5/my_project/_dsh/deepseek-harness`
- ❌ 不修改第三方仓库：`dsh-mobile`、`dsh-bridge` 及其副本
- ❌ 不修改用户的 DSH profile 配置（`~/.dsh/profiles/...`），除非用户在当次对话中明确要求
- ❌ 不改动 `/Users/lionm5/my_project/_dsh/` 下其他插件仓库

### 3.2 不做 DSH 侧的危险改动

- ❌ **不把 DSH 的 webserver `host` 改成 `0.0.0.0`**，也不通过 CLI `--host 0.0.0.0` 绕过官方限制。
  官方明确拒绝这个值（见 `docs/RESEARCH.md` §2.1）。本插件的存在意义就是**在不改 DSH 绑定的前提下**提供局域网访问。
- ❌ 不替换、不劫持官方 layout / slot 契约。官方 UI 必须原样复用。
  - **允许**：向官方**内容型 / 追加型** seat 注册自己的内容——`settings.section`（设置分区）与 `shell.overlay`（全帧浮层）。这是设置 UI（F6）的唯一允许做法。
  - **禁止**：替换官方**布局型** slot（`sidebar` / `rightbar` / `shell.leading`），或接管 `panelInfo` 等 layout 根 hook。
- ❌ 不修改 DSH 的会话日志、持久化格式、`~/.dsh` 下的用户数据。

### 3.3 不做未经授权的对外暴露

- ❌ 不允许出现「监听地址对外可达、但门禁未生效」的中间状态。门禁必须先于监听可用。
- ❌ 不实现任何公网隧道（Cloudflare / cpolar / FRP / Tailscale）、IM Bot 通道、端口转发。
  这些是 `docs/SPEC.md` 的**非目标**。
- ❌ 不在局域网用明文 HTTP 承载门禁密码（要么先上 TLS，要么在文档里显著标注风险并由用户确认）。

### 3.4 不做未经授权的发布与仓库操作

- ❌ 不执行 `npm publish`；
- ❌ 不创建 git tag、不建 GitHub Release；
- ❌ 不 `git init`、不创建远程仓库、不 `git push`——**除非用户在当次对话中明确要求**；
- ❌ 不合并任何分支到 `main`。

### 3.5 不引入风险

- ❌ 不把密码、明文凭据、launch token、cookie 签名 secret、证书私钥写进仓库或日志；
- ❌ 不引入未在 `docs/SPEC.md` 批准清单里的运行时依赖；
- ❌ 不直接复制 `dsh-mobile`（Apache-2.0）或 `dsh-bridge`（MIT）的源码。
  **只借鉴设计思路与参数**，实现自己写。若确需引用代码，先请求用户确认并处理许可声明。

---

## 4. 必须先问用户才能执行

以下是「灰名单」——不是禁止，但**必须拿到用户当次对话的明确同意**：

| 事项 | 为什么要问 |
| --- | --- |
| 引入新的运行时依赖 | 影响安装体积、供应链风险、许可 |
| 修改 `docs/GUARDRAILS.md`、`docs/SPEC.md`、`docs/PLAN.md`、`AGENTS.md` | 这是约束本身，改动等于改规则 |
| 改变架构决策（如决定替换官方 layout、引入独立 CA 管理、改用别的代理方式） | 偏离既定方案 |
| 删除任何文件 | 不可逆 |
| 任何形式的发布、推送、建仓 | 见 3.4 |
| 任何与 `docs/RESEARCH.md` 记录不符的实现 | 说明事实基线可能已过期，需先复核 |
| 对外部网络地址发起请求（除 npm 安装、查文档） | 隐私与可预期性 |
| 升级 DSH 基线版本 | 需要重跑 `docs/RESEARCH.md` 的验证清单 |

---

## 5. 何时执行（阶段门禁）

严格按 `docs/PLAN.md` 的阶段顺序推进，**不允许跳阶段、不允许并行做后面的阶段**。

每个阶段的三道门：

1. **进入条件**——`docs/PLAN.md` 里该阶段列出的前置产出必须已存在且通过验收；
2. **执行范围**——只做该阶段列出的工作项。发现需要做范围外的事，**记下来汇报，不要顺手做**；
3. **退出条件**——该阶段的验收命令全部通过，且 `CHANGELOG.md` 已更新。

### 两种运行模式

**默认：逐阶段模式**——每阶段结束停下汇报，等用户确认再进入下一阶段。

**连续实施模式**——**仅当用户在当次对话的启动语中明确授权**「连续实施直到完成」时启用：

- 阶段之间**不再停下等确认**，一个阶段的退出条件满足后立即进入下一个，直到全部必做阶段完成；
- 每个阶段的验收**必须真实跑通**。不通过就修；修不动则转为停下（见硬停点 4）；
- 每阶段结束**仍必须**输出四段汇报（做了什么 / 验证了什么 / 没验证了什么 / 下一步），只是不等回复就继续；
- **硬停点照旧生效**——连续模式不豁免任何一条。

### 硬停点（两种模式都必须停下）

1. **需要用户亲自做的事**——手机 / 平板实机验证、重启 dsh、发布批准；
2. **触碰红线或灰名单**——`AGENTS.md` §3 / §4 中的任何一条；
3. **需要产品决策**——`docs/SPEC.md` 未覆盖的取舍；
4. **同一问题连续两次未通过验收**——停下汇报，不要无限重试；
5. **发现文档自相矛盾或与 `docs/RESEARCH.md` 冲突**——先停下核对，不要凭猜测继续；
6. **全部必做阶段完成**——输出交付总结，并列出仍需用户做的事。

---

## 6. 工作流

### 改动前

1. 读本文件 → `docs/GUARDRAILS.md` → 当前阶段的 `docs/PLAN.md` 条目；
2. 若涉及 DSH 行为细节，**先核对 `docs/RESEARCH.md`**；若该文件没有，去读源码核实，**并把新事实补进 `docs/RESEARCH.md`**；
3. 明确本次改动的验收标准（哪条命令通过算完成）。

### 改动中

- 小步提交，一次一个职责；
- 不顺手重构无关代码；
- 新增行为必须有对应测试；
- 不修改测试来迁就实现——测试反映 `docs/SPEC.md`，不是反映现状。

### 改动后

1. 跑该阶段的验收命令（见 `docs/PLAN.md`）；
2. 更新 `CHANGELOG.md`；
3. 汇报四件事：**做了什么 / 验证了什么 / 没验证什么 / 下一步**；
4. **明确区分「源码确认」与「实机验证」**——不要把「我读了源码觉得对」说成「已验证可用」。

---

## 7. 漂移自检清单

每次汇报前，逐条自问。任何一条答「是」，立即停下汇报：

- [ ] 我是否改动了本仓库之外的任何文件？
- [ ] 我是否在没有用户明确同意的情况下，做了发布 / 推送 / 建仓 / 打 tag？
- [ ] 我是否重启了 dsh，或者诱导用户之外的进程重启 dsh？
- [ ] 我是否让监听对外可达，而门禁还没生效？
- [ ] 我是否开始实现 `docs/SPEC.md` 明确列为非目标的东西（隧道、IM Bot、官方布局替换）？
- [ ] 我是否凭记忆写了 DSH 的行为，而没有核对 `docs/RESEARCH.md` 或源码？
- [ ] 我是否跳过了阶段门禁，直接做了后面阶段的事？
- [ ] 我是否修改了 `docs/` 下的约束性文档而没有先问用户？
- [ ] 我是否把「源码确认」当成了「实机验证」来汇报？
- [ ] 我是否为了让验收通过而修改了测试或降低了断言强度？

---

## 8. 语言与沟通

- 与用户的所有交流、过程说明、汇报、提交信息：**中文**；
- 代码标识符、协议字段、文件路径：保留英文原样；
- `README.md` 用英文、`README.zh.md` 用中文（与既有插件一致）；
- 发布说明中英双语，结构见 `docs/RELEASE.md`。
